{
  "name": "whistl-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "agent",
    "whistl/app"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
