{
  "name": "@whistl/core",
  "version": "0.0.0",
  "private": true,
  "description": "Shared WHISTL backend — the canonical TxLINE client + types, consumed by all apps.",
  "exports": {
    "./txline/types": "./src/txline/types.ts",
    "./txline/server": "./src/txline/server.ts"
  }
}
